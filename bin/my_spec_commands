## Within a module directory
## puppet profile unit tests:
rake bundle_run[profile,spec]



## run a single test
bundle install --path vendor/gems
bundle exec rake spec_prep
bundle exec rake spec
bundle exec rspec <path/to/spec_file.rb>

